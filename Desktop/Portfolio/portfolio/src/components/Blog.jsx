import React from 'react';
import FontAwesome from 'react-fontawesome';
import { withRouter } from 'react-router-dom';

const Blog = () => {
    return (
        <div>
            <h2 className="headline">Things I've Written...</h2>
            
        </div>
    )
};

export default withRouter(Blog);