import React from 'react';
import FontAwesome from 'react-fontawesome';
import { withRouter } from 'react-router-dom';

const Projects = () => {
    return (
        <div>
            <h2 className="headline">Things I've done...</h2>
            
        </div>
    )
};

export default withRouter(Projects);