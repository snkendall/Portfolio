import React from 'react';
import FontAwesome from 'react-fontawesome';
import { withRouter } from 'react-router-dom';

const Talks = () => {
    return (
        <div>
            <h2 className="headline">Things I've Said...</h2>
            
        </div>
    )
};

export default withRouter(Talks);